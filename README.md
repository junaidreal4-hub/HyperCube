
  # HyperCube

  This is a code bundle for HyperCube. The original project is available at https://www.figma.com/design/HQUJmsFSkbkcz0uZLE4Trv/HyperCube.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  